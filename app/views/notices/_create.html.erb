<%#= f.error_messages %>
<div class="bordered-block">
	<h2>Register new notification</h2>
	<% if @open_notices.size >= 10 %>
	<p>Up to ten notifications can be made public at a time.</p>
	<% else %>
	<div class="form-group row">
		<div class="col-md-8">
			<%= f.text_area :message, rows: '5', value: '', class: 'elastic_textarea form-control' %>
			<div class="button-block">
				<%= render partial: 'layouts/buttons/create' %>
			</div>
		</div>
	</div>
	<% end %>
</div>
