<div class="report-card card">
  <h2>System access</h2>
  <!-- <%# if manager_report?(@course_role, @learners) %>
  <div class="card-right-top">
    <a data-toggle="collapse" href="#signin-report-details" aria-expanded="false" aria-controls="signin-report-details">
      <i class="fa fa-chevron-circle-down"></i> <%#= t('.show_learners_details') %>
    </a>
  </div>
  <%# end %> -->

  <table id="signins" class="table table-condensed">
    <thead>
      <tr>
        <th class="width160" rowspan="2"></th>
        <th class="width160 text-center" rowspan="2">Last Access</th>
        <th class="text-center" colspan="2">Access count</th>
      </tr>
      <tr>
        <th class="width160 text-center">1 to 7 days ago</th>
        <th class="width160 text-center">8 to 14 days ago</th>
      </tr>
    </thead>

    <tbody>
      <%= render partial: 'signin_report', collection: signin_reports(@learners) %>
    </tbody>

    <!-- <%# if manager_report?("manager", @learners) %>
    <%# if manager_report?(@course_role, @learners) %>
    <tbody id="signin-report-details" class="collapse">
    <%#= render partial: 'signin_report', collection: toggle_signin_reports(@learners) %>
    </tbody>
    <%# end %> -->

  </table>
</div>
